{
  "name": "rooster-fetcher",
  "version": "1.0.0",
  "description": "Fetches Eduflex + Magister schedule and writes rooster.json",
  "main": "fetch-all.js",
  "scripts": {
    "fetch": "node fetch-all.js"
  },
  "dependencies": {
    "puppeteer": "^22.0.0"
  }
}
